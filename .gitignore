# Secrets & env
*.env
ck-secrets.env
.github-token

# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# Internal session notes — must NOT be served publicly (publish = ".")
Handoffs/
Knowledge/

# Backups / scratch
*.bak
ck_test.js

# Netlify deploy build artifacts
deploy-*.zip

# Session notes saved to root (Knowledge/ & Handoffs/ dirs broken on mount)
2026-*_CK_*_s2.md
notes2/
